__pycache__/
*.egg-info/
.DS_Store
.jarvis/
.pytest_cache/
.venv-pocket/
.venv-rvc/
.venv-tts/
.venv-xtts/
.venv/
*.pyc
# Personal/local JARVIS voice model — never commit (drop-in dir keeps README only).
voice_data/
voice_models/*.index
voice_models/*.onnx
voice_models/*.pth
voice_models/*.wav

# PyInstaller build output (regenerated by packaging/build_*.sh)
/build/
/dist/
*.spec.bak
python-3.11.9-amd64.exe

# 루트의 음성 샘플 산출물(재생성 가능) — 저장소 비대 방지
/jarvis_*.wav

# CI 빌드에 필요한 소형 자산은 추적한다(대형 4종은 GitHub Release 자산)
!voice_models/silero_vad.onnx
!voice_models/jarvis_en_ref.wav
!voice_models/jarvis_ref.wav

# 브레인스토밍 비주얼 컴패니언 산출물(로컬 전용)
.superpowers/
docs/이어서_작업.md
