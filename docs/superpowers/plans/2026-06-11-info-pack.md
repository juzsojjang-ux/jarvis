# 정보 팩(3b) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 음성 비서 특화 정보 기능 3종 — 통역 모드(한↔영 실시간), 메시지·메일 읽기 도구, 자동 기억 강화.

**Architecture:** 통역은 `jarvis/core/interpret.py`(언어감지+한국어 say 출력) + `SubscriptionBrain.translate`(도구 없는 1회 질의) + 오케스트레이터 라우팅(`interpret_mode`). 메시지/메일은 jarvis_mcp 읽기 도구 2개. 자동 기억은 두뇌 지침 1문장 + `MemoryStore.remember` 중복 방지. 전부 기존 패턴(주입형 runner, 음성 게이트, TDD).

**Tech Stack:** macOS `say`(한국어 통역 출력), osascript AppleScript(Messages/Mail), claude-agent-sdk(번역 질의), 기존 mlx-whisper(자동 언어감지).

**스펙:** `docs/superpowers/specs/2026-06-11-info-pack-design.md`

**프로젝트 약속:** 테스트 `cd ~/jarvis && .venv/bin/python -m pytest <path> -v` / 린트 `.venv/bin/ruff check jarvis tests`(line 100) / 커밋 한국어 + `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` 푸터 / 주석은 '왜'만 / **라이브 앱 재시작 금지(컨트롤러 담당), 테스트는 실제 say/osascript/SDK 절대 호출 금지(주입)**.

## 파일 구조

| 파일 | 책임 |
|---|---|
| Create `jarvis/core/interpret.py` | `detect_lang` + `interpret_speak_korean` |
| Modify `jarvis/brain/memory.py` | `remember` 중복 방지 |
| Modify `jarvis/brain/subscription.py` | `translate()` 메서드 + 자동 기억 지침 1문장 |
| Modify `jarvis/tools/jarvis_mcp.py` | `messages_text`/`mail_text` 도구 2개 |
| Modify `jarvis/core/config.py` | `interpret_enabled`/`interpret_ko_voice` |
| Modify `jarvis/core/orchestrator.py` | `interpret_mode`/`_interpret_command`/`_interpret_turn` + STT 자동감지 |
| Test | `tests/core/test_interpret.py`(신규), test_memory/test_subscription/test_jarvis_mcp/test_config_m2/test_orchestrator 확장 |

---

### Task 1: 언어 감지 + 한국어 음성 출력

**Files:**
- Create: `jarvis/core/interpret.py`
- Test: `tests/core/test_interpret.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/core/test_interpret.py
from types import SimpleNamespace

from jarvis.core.interpret import detect_lang, interpret_speak_korean


def test_detect_lang_korean():
    assert detect_lang("안녕하세요") == "ko"
    assert detect_lang("자비스 오늘 날씨") == "ko"


def test_detect_lang_english():
    assert detect_lang("hello there") == "en"
    assert detect_lang("what time is it") == "en"


def test_detect_lang_mixed_is_korean():
    # 한글이 하나라도 있으면 한국어로 본다(주인님은 한국어 화자).
    assert detect_lang("ok 그래") == "ko"


def test_detect_lang_empty_is_english():
    assert detect_lang("") == "en"
    assert detect_lang("12345 !!!") == "en"


def test_speak_korean_invokes_say():
    calls = []

    def runner(cmd, capture_output=True, text=True, timeout=None):
        calls.append(cmd)
        return SimpleNamespace(returncode=0)

    interpret_speak_korean("안녕하세요", voice="Yuna", runner=runner)
    assert calls == [["say", "-v", "Yuna", "안녕하세요"]]


def test_speak_korean_swallows_failure():
    def boom(cmd, capture_output=True, text=True, timeout=None):
        raise RuntimeError("say missing")

    interpret_speak_korean("안녕", runner=boom)  # raise하지 않아야 한다
```

- [ ] **Step 2: 실패 확인** — `.venv/bin/python -m pytest tests/core/test_interpret.py -v` → ModuleNotFoundError

- [ ] **Step 3: 구현**

```python
# jarvis/core/interpret.py
"""통역 모드 보조: 언어 감지(한글 음절 존재)와 한국어 음성 출력(macOS say).
현 TTS는 Pocket 영어 전용이라 통역의 한국어 방향은 say로 직접 재생한다."""
from __future__ import annotations

import subprocess


def detect_lang(text: str) -> str:
    """한글 음절(가–힣)이 하나라도 있으면 'ko', 아니면 'en'. 주인님은 한국어
    화자라 혼합 발화는 한국어로 본다(영어로 통역해 주는 게 자연스럽다)."""
    if any("가" <= ch <= "힣" for ch in (text or "")):
        return "ko"
    return "en"


def interpret_speak_korean(text: str, voice: str = "Yuna",
                           runner=subprocess.run) -> None:
    """macOS say로 한국어를 기본 출력장치에 재생. 실패는 조용히 무시(통역
    한 줄이 모드를 깨면 안 된다)."""
    text = (text or "").strip()
    if not text:
        return
    try:
        runner(["say", "-v", voice, text], capture_output=True, text=True, timeout=30)
    except Exception:  # noqa: BLE001 - 음성 출력 실패가 통역 모드를 멈추면 안 된다
        pass
```

- [ ] **Step 4: 통과 확인** — 6 PASS, ruff clean
- [ ] **Step 5: Commit** — `feat(interpret): 언어 감지 + 한국어 say 출력` (+푸터)

---

### Task 2: MemoryStore 중복 방지

**Files:**
- Modify: `jarvis/brain/memory.py`
- Modify: `tests/test_memory.py`

- [ ] **Step 1: 실패하는 테스트 추가** — `tests/test_memory.py` 끝에 (기존 임시파일 패턴 확인 후 맞춤; 아래는 tmp_path 사용):

```python
def test_remember_skips_duplicate(tmp_path):
    from jarvis.brain.memory import MemoryStore
    m = MemoryStore(tmp_path / "mem.md")
    m.load()
    m.remember("주인님은 매운 음식을 못 먹는다")
    m.remember("주인님은 매운 음식을 못 먹는다")          # 완전 중복
    m.remember("주인님은  매운 음식을 못먹는다!!")         # 공백·구두점만 다름
    assert m.text().count("매운 음식") == 1               # 한 번만 저장


def test_remember_substring_duplicate(tmp_path):
    from jarvis.brain.memory import MemoryStore
    m = MemoryStore(tmp_path / "mem.md")
    m.load()
    m.remember("커피는 아메리카노")
    m.remember("커피는 아메리카노만 마신다")              # 기존을 포함 → 중복 취급
    assert m.text().count("아메리카노") == 1


def test_remember_distinct_kept(tmp_path):
    from jarvis.brain.memory import MemoryStore
    m = MemoryStore(tmp_path / "mem.md")
    m.load()
    m.remember("이름은 이성재")
    m.remember("생일은 3월")
    assert "이성재" in m.text() and "3월" in m.text()
```

- [ ] **Step 2: 실패 확인** — `test_remember_skips_duplicate` FAIL (중복 두 줄 저장됨)

- [ ] **Step 3: 구현** — `jarvis/brain/memory.py`. 파일 상단 import에 `import re` 추가(없으면). `remember` 메서드를 다음으로 교체:

```python
    def remember(self, note: str) -> None:
        note = note.strip()
        if not note:
            return
        if self._is_duplicate(note):  # 같은/포함 관계 사실은 다시 적지 않는다
            return
        self._path.parent.mkdir(parents=True, exist_ok=True)
        line = f"- {note}\n"
        with self._path.open("a", encoding="utf-8") as f:
            f.write(line)
        self._text = (self._text + line) if self._text else line

    @staticmethod
    def _norm(s: str) -> str:
        # 비교용 정규화: 소문자·공백 축약·구두점 제거. 의미 같은데 표기만 다른
        # 중복을 잡기 위함(완벽한 의미비교가 아니라 값싼 휴리스틱).
        s = re.sub(r"[^\w\s]", "", s.lower())
        return re.sub(r"\s+", " ", s).strip()

    def _is_duplicate(self, note: str) -> bool:
        n = self._norm(note)
        if not n:
            return False
        for line in self._text.splitlines():
            existing = self._norm(line.lstrip("- ").strip())
            if not existing:
                continue
            if n == existing or n in existing or existing in n:
                return True
        return False
```

- [ ] **Step 4: 통과 확인** — `tests/test_memory.py` 전체 PASS
- [ ] **Step 5: Commit** — `feat(memory): remember 중복 방지(정규화 부분일치)` (+푸터)

---

### Task 3: 설정 2종

**Files:**
- Modify: `jarvis/core/config.py`
- Modify: `tests/core/test_config_m2.py`

- [ ] **Step 1: 실패하는 테스트 추가** — `tests/core/test_config_m2.py` 끝에:

```python
def test_interpret_defaults():
    s = Settings()
    assert s.interpret_enabled is True
    assert s.interpret_ko_voice == "Yuna"
```

- [ ] **Step 2: 실패 확인** — AttributeError
- [ ] **Step 3: 구현** — `jarvis/core/config.py`의 M4 능동 블록 아래(HUD 블록 위)에:

```python
    # --- M5 정보 팩 (3b) ---
    interpret_enabled: bool = True        # "통역 모드" 사용 가능
    interpret_ko_voice: str = "Yuna"      # 통역 한국어 출력 보이스(macOS say)
```

- [ ] **Step 4: 통과 확인** — config 테스트 전체 PASS
- [ ] **Step 5: Commit** — `feat(config): 통역 모드 설정 추가` (+푸터)

---

### Task 4: SubscriptionBrain.translate

**Files:**
- Modify: `jarvis/brain/subscription.py`
- Modify: `tests/brain/test_subscription.py`

번역은 도구 없는 1회 질의. 기존 영속 클라이언트(`_ensure_client`)와 별개로,
주입형 `_client_cls`/`_options_cls`를 재사용하되 새 클라이언트를 열고 닫는다.

- [ ] **Step 1: 실패하는 테스트 추가** — `tests/brain/test_subscription.py`. 파일에 이미 있는 가짜 SDK 헬퍼/픽스처를 확인하고 맞추되, 없으면 아래 자족형 가짜로:

```python
def test_translate_uses_translation_only_options():
    import asyncio

    from jarvis.brain.subscription import SubscriptionBrain
    from jarvis.core.config import Settings

    captured = {}

    class _FakeOptions:
        def __init__(self, **kw):
            captured.update(kw)

    class _FakeClient:
        def __init__(self, options=None):
            captured["options_obj"] = options

        async def __aenter__(self):
            return self

        async def __aexit__(self, *a):
            return False

        async def query(self, text):
            captured["query"] = text

        async def receive_response(self):
            class _Blk:
                type = "text"
                text = "Hello, sir."

            class _Msg:
                content = [_Blk()]
            yield _Msg()

    brain = SubscriptionBrain(Settings(), None, "p" * 4096,
                              client_cls=_FakeClient, options_cls=_FakeOptions)
    out = asyncio.run(brain.translate("안녕하세요", "English"))
    assert out == "Hello, sir."
    assert captured["query"] == "안녕하세요"
    assert captured["allowed_tools"] == []          # 도구 없음
    assert captured["max_turns"] == 1
    assert "English" in captured["system_prompt"]
```

(주의: 파일에 이미 SubscriptionBrain 가짜-SDK 테스트가 있으면 그 메시지/이벤트
형태에 맞춰 `receive_response`가 내놓는 객체 모양을 일치시켜라. 실제 brain의
스트림 파싱이 `AssistantMessage`+`StreamEvent` 기반이면, 이 테스트의 `_Msg`는
`_assistant_message` 주입으로 대체하거나, translate가 자체적으로 텍스트 블록만
모으도록 단순 구현한다. 아래 구현은 텍스트 블록 수집 방식이라 위 가짜로 충분.)

- [ ] **Step 2: 실패 확인** — AttributeError translate

- [ ] **Step 3: 구현** — `subscription.py`에 메서드 추가(`respond` 근처):

```python
    async def translate(self, text: str, target_lang: str) -> str:
        """도구 없는 1회 번역 질의. 번역문만 돌려준다(설명·따옴표 없음)."""
        self._lazy_import()  # _client_cls/_options_cls 보장(기존 헬퍼 이름에 맞춰라)
        env = {k: v for k, v in os.environ.items() if k != "ANTHROPIC_API_KEY"}
        opts = self._options_cls(
            system_prompt=(f"Translate the given sentence into {target_lang}. "
                           "Output ONLY the translation — no explanation, quotes, "
                           "or notes."),
            allowed_tools=[],
            setting_sources=[],
            max_turns=1,
            env=env,
        )
        out: list[str] = []
        async with self._client_cls(options=opts) as client:
            await client.query(text)
            async for msg in client.receive_response():
                for block in getattr(msg, "content", []) or []:
                    if getattr(block, "type", "") == "text":
                        out.append(getattr(block, "text", ""))
        return "".join(out).strip()
```

주의: `self._lazy_import()`는 SDK import + `_client_cls`/`_options_cls` 기본값을
세팅하는 기존 비공개 메서드의 실제 이름으로 바꿔라(현 코드에서 `_ensure_client`
또는 `_options` 진입 시 import하는 부분 — 구현자가 파일을 읽고 정확한 호출로
맞춘다. 가짜 주입 테스트에서는 `_client_cls`/`_options_cls`가 이미 세팅돼 있어
import 경로가 타지 않는다).

- [ ] **Step 4: 자동 기억 지침 추가** — `_GUIDANCE_EN`과 `_GUIDANCE_KO` 각각에 1문장.
`_GUIDANCE_EN`의 [SYSTEM EVENT] 문장 근처에:

```python
    "When you naturally learn a durable personal fact about sir during the chat "
    "(a preference, a commitment, a name, a recurring habit) that would help you "
    "later, quietly call the remember tool — do not ask. Never store small talk, "
    "transient context, or sensitive data. "
```

`_GUIDANCE_KO`에도 대응:

```python
    "대화 중 주인님의 지속적인 개인 정보(선호·약속·이름·반복 습관)를 자연스럽게 "
    "알게 되면 다음에 유용하니 묻지 말고 remember 도구로 조용히 저장하라. 잡담· "
    "일시적 맥락·민감정보는 저장하지 마라. "
```

- [ ] **Step 5: 통과 확인** — `tests/brain/` 전체 + `tests/brain/test_subscription.py` PASS, ruff clean. (자동 기억 지침은 기존 위트/[KO]/[SYSTEM EVENT] 문장을 깨면 안 됨 — 지침 테스트가 있으면 통과 확인.)
- [ ] **Step 6: Commit** — `feat(brain): translate 1회 질의 + 자동 기억 지침` (+푸터)

---

### Task 5: 메시지·메일 읽기 도구

**Files:**
- Modify: `jarvis/tools/jarvis_mcp.py`
- Modify: `tests/tools/test_jarvis_mcp.py`

- [ ] **Step 1: 실패하는 테스트 추가** (`_recording_runner` 재사용):

```python
def test_messages_text_lists_recent():
    from jarvis.tools.jarvis_mcp import messages_text
    r = _recording_runner(stdout="민지|도착했어\n부장님|회의 30분 뒤\n")
    out = messages_text(2, runner=r)
    assert "민지" in out and "도착했어" in out and "부장님" in out


def test_messages_text_empty():
    from jarvis.tools.jarvis_mcp import messages_text
    assert "없습니다" in messages_text(5, runner=_recording_runner(stdout=""))


def test_messages_text_runner_error_safe():
    from jarvis.tools.jarvis_mcp import messages_text

    def boom(cmd, capture_output=True, text=True, timeout=None, input=None):
        raise RuntimeError("osascript fail")

    out = messages_text(5, runner=boom)
    assert "읽지" in out or "없습니다" in out          # raise 금지


def test_mail_text_lists_unread():
    from jarvis.tools.jarvis_mcp import mail_text
    r = _recording_runner(stdout="김부장|회의 일정\nAWS|결제 안내\n")
    out = mail_text(2, runner=r)
    assert "김부장" in out and "회의 일정" in out


def test_mail_text_empty():
    from jarvis.tools.jarvis_mcp import mail_text
    assert "없습니다" in mail_text(5, runner=_recording_runner(stdout=""))


def test_read_tools_registered():
    from jarvis.tools.jarvis_mcp import JARVIS_TOOL_NAMES
    assert "mcp__jarvis__get_messages" in JARVIS_TOOL_NAMES
    assert "mcp__jarvis__get_unread_mail" in JARVIS_TOOL_NAMES
```

- [ ] **Step 2: 실패 확인** — ImportError messages_text

- [ ] **Step 3: 구현** — jarvis_mcp.py:

```python
_MESSAGES_SCRIPT = """
set out to ""
tell application "Messages"
    set msgs to «class incm» of (every chat)
end tell
return out
"""

# Messages의 안정적 스크립팅이 제한적이라, 받은 메시지는 chat.db를 sqlite로 읽는
# 편이 견고하다. 다만 권한·스키마 변동을 피하려 v1은 osascript 기반으로 가되,
# 환경에 따라 빈 결과가 날 수 있음을 도구 설명에 명시한다.
_MESSAGES_OSA = (
    'tell application "Messages" to get «class incm»'  # placeholder — 아래 함수가 실제 처리
)


def _parse_pipe_lines(raw: str, limit: int) -> list[tuple[str, str]]:
    out: list[tuple[str, str]] = []
    for line in (raw or "").splitlines():
        parts = line.split("|", 1)
        if len(parts) == 2 and parts[0].strip():
            out.append((parts[0].strip(), parts[1].strip()))
        if len(out) >= limit:
            break
    return out


def messages_text(count: Any = 5, runner=subprocess.run) -> str:
    try:
        n = max(1, min(20, int(count)))
    except (TypeError, ValueError):
        n = 5
    script = (
        'set out to ""\n'
        'tell application "Messages"\n'
        '  set theChats to chats\n'
        '  repeat with c in theChats\n'
        '    try\n'
        '      set m to last text message of c\n'
        '      set out to out & (get name of c) & "|" & (get text of m) & linefeed\n'
        '    end try\n'
        '  end repeat\n'
        'end tell\n'
        'return out\n'
    )
    try:
        res = runner(["osascript", "-e", script], capture_output=True, text=True,
                     timeout=15)
        items = _parse_pipe_lines(getattr(res, "stdout", "") or "", n)
    except Exception:  # noqa: BLE001 - 권한·앱부재: 안내만
        return "메시지를 읽지 못했습니다(권한을 확인해 주세요)."
    if not items:
        return "최근 메시지가 없습니다."
    return "최근 메시지 " + " / ".join(f"{who}: {body}" for who, body in items)


def mail_text(count: Any = 5, runner=subprocess.run) -> str:
    try:
        n = max(1, min(20, int(count)))
    except (TypeError, ValueError):
        n = 5
    script = (
        'set out to ""\n'
        'tell application "Mail"\n'
        '  set unread to (messages of inbox whose read status is false)\n'
        '  repeat with m in unread\n'
        '    set out to out & (sender of m) & "|" & (subject of m) & linefeed\n'
        '  end repeat\n'
        'end tell\n'
        'return out\n'
    )
    try:
        res = runner(["osascript", "-e", script], capture_output=True, text=True,
                     timeout=15)
        items = _parse_pipe_lines(getattr(res, "stdout", "") or "", n)
    except Exception:  # noqa: BLE001
        return "메일을 읽지 못했습니다(권한을 확인해 주세요)."
    if not items:
        return "안 읽은 메일이 없습니다."
    return f"안 읽은 메일 {len(items)}건: " + " / ".join(
        f"{who} — {subj}" for who, subj in items)
```

(주의: 위의 `_MESSAGES_SCRIPT`/`_MESSAGES_OSA` placeholder 상수는 넣지 말 것 —
실제 사용하는 건 `messages_text` 내부의 `script` 문자열뿐이다. 구현자는
placeholder 두 상수를 생략하고 `_parse_pipe_lines`+두 함수만 추가한다.)

@tool 래퍼:

```python
@tool("get_messages", "최근 받은 메시지를 읽는다(읽기 전용). 보내기는 하지 않는다.",
      {"type": "object", "properties": {"count": {"type": "integer"}}})
async def _get_messages(args):
    return _text(messages_text((args or {}).get("count", 5)))


@tool("get_unread_mail", "안 읽은 메일의 발신자·제목을 읽는다(읽기 전용).",
      {"type": "object", "properties": {"count": {"type": "integer"}}})
async def _get_unread_mail(args):
    return _text(mail_text((args or {}).get("count", 5)))
```

tools 리스트 + JARVIS_TOOL_NAMES에 `_get_messages, _get_unread_mail` / `"get_messages", "get_unread_mail"` 등록.

- [ ] **Step 4: 통과 확인** — `tests/tools/test_jarvis_mcp.py` 전체 PASS, ruff
- [ ] **Step 5: Commit** — `feat(tools): 메시지/메일 읽기 도구(읽기 전용)` (+푸터)

---

### Task 6: 오케스트레이터 통역 라우팅

**Files:**
- Modify: `jarvis/core/orchestrator.py`
- Modify: `tests/test_orchestrator.py`

- [ ] **Step 1: 실패하는 테스트 추가** — `tests/test_orchestrator.py` 끝에. (기존 `_make()`/`_FakeBrain`/`_FakeSTT`/`_FakePlayback` 재사용; 통역엔 brain.translate와 say runner를 주입한다.)

```python
class _XlateBrain:
    async def respond(self, user_text):           # 통역 모드에선 안 불려야 한다
        raise AssertionError("respond called in interpret mode")
        yield

    async def translate(self, text, target):
        return f"<{target}:{text}>"


def _interp(orch):
    orch.brain = _XlateBrain()
    orch.interpret_mode = True


def test_interpret_command_toggles_on_off():
    orch, pb = _make()

    async def run():
        await orch._pipeline_text("통역 모드 켜줘")
        on = orch.interpret_mode
        await orch._pipeline_text("통역 모드 꺼줘")
        return on, orch.interpret_mode

    on, off = asyncio.run(run())
    assert on is True and off is False
    assert len(pb.feeds) >= 1                       # 켜짐 안내 발화


def test_interpret_korean_input_speaks_english():
    orch, pb = _make()
    _interp(orch)

    async def run():
        await orch._pipeline_text("안녕하세요")
    asyncio.run(run())
    # 영어 방향 → Pocket _speak 경로(playback.feed 발생)
    assert len(pb.feeds) >= 1
    assert orch.state == State.IDLE


def test_interpret_english_input_speaks_korean(monkeypatch):
    orch, pb = _make()
    _interp(orch)
    spoken_ko = []

    def fake_say(text, voice="Yuna", runner=None):
        spoken_ko.append((text, voice))

    monkeypatch.setattr("jarvis.core.orchestrator.interpret_speak_korean", fake_say)

    async def run():
        await orch._pipeline_text("hello there")
    asyncio.run(run())
    assert spoken_ko and "hello there" in spoken_ko[0][0]   # <한국어:hello there>
    assert pb.feeds == []                                    # Pocket 경로 안 탐


def test_interpret_translate_failure_recovers(monkeypatch):
    orch, pb = _make()

    class _BoomXlate:
        async def respond(self, user_text):
            raise AssertionError
            yield

        async def translate(self, text, target):
            raise RuntimeError("api down")

    orch.brain = _BoomXlate()
    orch.interpret_mode = True

    async def run():
        await orch._pipeline_text("안녕")
    asyncio.run(run())
    assert orch.state == State.IDLE                          # 모드 유지하되 IDLE 복귀
    assert orch.interpret_mode is True
```

- [ ] **Step 2: 실패 확인** — AttributeError interpret_mode / `_interpret_command`

- [ ] **Step 3: 구현** — orchestrator.py:

3a. import 추가:
```python
from .interpret import detect_lang, interpret_speak_korean
```

3b. `__init__`에 `self.proactive = None` 근처:
```python
        self.interpret_mode = False  # "통역 모드" — 두뇌 우회, 한↔영 통역만
```

3c. `_pipeline_text` 진입부(빈 텍스트 체크 뒤, THINKING 전)에 통역 분기:
```python
    async def _pipeline_text(self, text: str, *, ack: bool = True) -> None:
        if not text.strip():
            self._to_idle()
            return
        cmd = self._interpret_command(text)
        if cmd is not None:
            await self._toggle_interpret(cmd)
            return
        if self.interpret_mode:
            await self._interpret_turn(text)
            return
        self.state = State.THINKING
        ...(기존 그대로)...
```

3d. 메서드 추가(`_attentive_expiry` 아래쯤):
```python
    # ----- 통역 모드 -----
    _INTERP_ON = ("켜", "시작", "on")
    _INTERP_OFF = ("꺼", "끄", "종료", "off", "그만")

    def _interpret_command(self, text: str) -> str | None:
        if "통역" not in text:
            return None
        if any(w in text for w in self._INTERP_OFF):
            return "off"
        if any(w in text for w in self._INTERP_ON):
            return "on"
        return None

    async def _toggle_interpret(self, cmd: str) -> None:
        self.interpret_mode = (cmd == "on")
        en, ko = (("Interpreter mode on, sir.", "통역 모드를 켰습니다.")
                  if self.interpret_mode
                  else ("Interpreter mode off, sir.", "통역 모드를 껐습니다."))
        await self._play_phrase(en, ko)
        await self._finish_speaking("")
        self.state = State.IDLE
        if self.wake is not None:
            self._enter_attentive()
        else:
            self._publish("idle")

    async def _interpret_turn(self, text: str) -> None:
        try:
            src = detect_lang(text)
            if src == "ko":
                out = await self.brain.translate(text, "English")
                await self._speak(out)              # Pocket 영어 음성
                await self._finish_speaking("")
            else:
                out = await self.brain.translate(text, "Korean")
                await asyncio.to_thread(
                    interpret_speak_korean, out, self.settings.interpret_ko_voice)
        except Exception as exc:  # noqa: BLE001 - 통역 한 줄 실패가 모드를 깨면 안 된다
            print(f"[통역] 오류: {exc}")
        self.state = State.IDLE
        if self.wake is not None:
            self._enter_attentive()                 # 연속 통역
        else:
            self._publish("idle")
```

3e. `_pipeline`의 STT 호출 — 통역 모드면 자동 언어감지:
```python
    async def _pipeline(self, pcm: np.ndarray) -> None:
        try:
            lang = None if self.interpret_mode else self.settings.language
            text = await asyncio.to_thread(self.stt.transcribe, pcm, 16000, lang)
            await self._pipeline_text(text)
        except Exception as exc:  # noqa: BLE001
            print(f"[파이프라인] 오류(IDLE 복귀): {exc}")
            self._to_idle()
```

(주의: 기존 `_FakeSTT.transcribe(self, pcm, sample_rate=16000, language="ko")`는
language=None을 받아도 동작해야 한다 — 가짜는 무시하므로 OK.)

- [ ] **Step 4: 통과 확인** — `tests/test_orchestrator.py` 전체(기존+신규) PASS. 특히
기존 PTT/웨이크/announce 테스트가 깨지지 않아야 한다(통역 분기는 interpret_mode
False면 무영향). ruff clean.
- [ ] **Step 5: Commit** — `feat(interpret): 오케스트레이터 통역 라우팅 — 토글/한↔영/연속` (+푸터)

---

### Task 7: 전체 검증 (라이브는 컨트롤러)

- [ ] **Step 1:** `.venv/bin/python -m pytest -q` 전체 PASS, `.venv/bin/ruff check jarvis tests` clean
- [ ] **Step 2 (컨트롤러):** 재시작 후 라이브 체크: ① "통역 모드 켜줘" → "hello"라 말하면 한국어 say로, "안녕하세요"라 말하면 영어 Pocket으로 ② "통역 모드 꺼줘" → 평소 복귀 ③ "안 읽은 메일 알려줘" / "메시지 온 거 있어?"(권한 팝업 허용) ④ 대화 중 "나 매운 거 못 먹어" → 자동 기억(이후 ~/.jarvis/memory.md 확인) ⑤ 같은 사실 다시 말해도 중복 저장 안 됨
- [ ] **Step 3 (컨트롤러):** 메모리 업데이트

## 셀프리뷰 결과

- **스펙 커버리지**: A 통역(T1 detect/say·T4 translate·T6 라우팅+토글+연속+STT자동감지+ko_voice) ✓ / B 메시지·메일(T5, 읽기전용·주입runner·raise금지·등록) ✓ / C 자동 기억(T2 중복방지·T4 지침1문장) ✓ / 설정 2종(T3) ✓ / 에러처리(통역 실패 IDLE+모드유지 T6, 도구 안내문자열 T5) ✓.
- **타입 일치**: `detect_lang(text)->str`·`interpret_speak_korean(text,voice,runner)` T1↔T6 / `brain.translate(text,target)->str` T4↔T6 / `messages_text(count,runner)`·`mail_text` T5 / `MemoryStore.remember` 시그니처 불변 T2 / `interpret_mode` bool·`_interpret_command->on|off|None` T6 일관 ✓.
- **플레이스홀더 없음**: T5의 `_MESSAGES_SCRIPT`/`_MESSAGES_OSA` placeholder는 "넣지 말 것"으로 명시 — 실제 코드는 함수 내 script 문자열. 나머지 완결.
- **유의(구현자 확인 필요)**: ① T4의 `_lazy_import()`는 실제 비공개 메서드명으로 교체(현 subscription.py가 SDK import하는 지점 확인). ② T6 monkeypatch가 `jarvis.core.orchestrator.interpret_speak_korean`를 패치하므로 orchestrator는 모듈 최상위에서 import해야 함(`from .interpret import ...`).
